tolgee:
  postgres-autostart:
    enabled: false
  authentication:
    enabled: true
    initial-username: admin
spring:
  datasource:
    url: jdbc:postgresql://db:5432/postgres
    username: postgres
    password: {postgresPassword}
